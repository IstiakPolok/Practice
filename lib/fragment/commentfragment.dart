import 'package:flutter/cupertino.dart';

class commentfragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("This is commentfragment")),
    );
  }
}