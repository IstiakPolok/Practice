import 'package:flutter/cupertino.dart';

class accessfragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("This is accessfragment")),
    );

  }
}
