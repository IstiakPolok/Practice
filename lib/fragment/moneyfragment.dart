import 'package:flutter/cupertino.dart';

class moneyfragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("This is moneyfragment")),
    );
  }
}